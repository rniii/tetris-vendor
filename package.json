{
    "type": "module",
    "scripts": {
        "start": "esbuild src/index.ts --bundle --format=esm --packages=external | node --env-file=.env -"
    },
    "devDependencies": {
        "@types/node": "^24.0.15",
        "esbuild": "^0.25.9"
    },
    "dependencies": {
        "oceanic.js": "^1.12.0",
        "tetris": "link:tetris"
    }
}
